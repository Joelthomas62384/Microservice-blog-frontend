export interface RegisterFormData {
    email: string;
    full_name: string;
    username: string;
    password: string;
  };

export interface LoginFormData {
    email: string;
    password: string;
  }